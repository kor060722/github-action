name: Dev Workflows
on:
  push:
    branches:
    - main
jobs:
  dev-job-first:
    name: dev-job-first
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        repository: dml113/github-action
    - run: cat *
