name: Dev Workflows
on:
  push:
    branches:
    - main
jobs:
  dev-job-first:
    name: dev-job-first
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        repository: dml113/github-action
        token: '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'
    - run: cat *
